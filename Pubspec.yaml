name: the_money_world
description: The Money World - L'argent du monde, prix reel de l'or et des devises
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.5
  http: ^1.1.0

flutter:
  uses-material-design: true
